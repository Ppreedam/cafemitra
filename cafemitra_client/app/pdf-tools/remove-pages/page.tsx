import type { Metadata } from "next";
import type { ReactNode } from "react";
import RemovePagesClient from "./RemovePagesClient";

const pageUrl = "https://repetigo.com/pdf-tools/remove-pages";

export const metadata: Metadata = {
  title: "Remove Pages from PDF - Free Online, No Sign-Up | RepetiGo",
  description:
    "Remove pages from a PDF free and online - no sign-up, no software. Delete single, multiple, or blank pages on any device. Files auto-delete within 60 minutes.",
  alternates: {
    canonical: pageUrl,
  },
  openGraph: {
    title: "Remove Pages from PDF - Free Online, No Sign-Up | RepetiGo",
    description: "Delete pages from a PDF free in your browser. No sign-up. Files auto-delete within 60 minutes.",
    type: "website",
    url: pageUrl,
  },
  twitter: {
    card: "summary_large_image",
    title: "Remove Pages from PDF - Free Online | RepetiGo",
    description: "Free online tool to delete pages from a PDF on any device. No sign-up. Auto-delete.",
  },
  robots: {
    index: true,
    follow: true,
  },
};

const content = String.raw`H1: Remove Pages from PDF - Free, Online, No Sign-Up
Need to delete a page from a PDF? Upload your file, pick the pages you don't want, and download a clean new PDF - all in your browser. No account, no software install, and no watermark on the result. RepetiGo's tool to remove pages from a PDF is free, works on any device, and deletes your file automatically within 60 minutes of processing.
✓ 100% free      ✓ No sign-up      ✓ Works on any device      ✓ Auto-deletes in 60 min
➜ Remove Pages from PDF - Free
No email. No install. Just upload and delete.

H2: What Does Removing Pages from a PDF Mean?
Removing pages from a PDF means permanently deleting one or more pages from a PDF document so they no longer appear in the file. RepetiGo's remove pages from PDF tool is a free, browser-based utility that lets you delete unwanted pages - a blank page, a duplicate scan, a cover sheet, or any range you choose - and save a smaller, cleaner PDF in seconds.
Unlike editing the whole document, you are not changing the text or layout of the remaining pages. You are simply dropping the pages you don't need. This is one of the most common PDF tasks in offices, colleges, and print shops - and it should not require a paid Adobe Acrobat licence or a desktop program.
📊 Every file you upload runs through an ephemeral pipeline and is automatically deleted within 60 minutes. RepetiGo never stores your PDF, and no sign-up is required to remove pages from a PDF.

H2: How to Remove Pages from a PDF (Step by Step)
Here is how to remove pages from a PDF for free with RepetiGo. The whole process takes under a minute:
1. Upload your PDF. Drag and drop the file, or click to browse. It uploads over an encrypted connection.
2. Select the pages to delete. Your PDF appears as page thumbnails. Click any page - single, multiple, or a range - to mark it for removal.
3. Click 'Remove Pages'. RepetiGo deletes the selected pages and rebuilds a clean PDF instantly.
4. Download your new PDF. Save the smaller file to your device. Your original is auto-deleted within 60 minutes.
💡 You can remove a page from a PDF and keep everything else intact - the remaining pages keep their original quality, order, and formatting.

H2: Is It Safe to Remove Pages from a PDF Online?
Yes - if the tool handles your file responsibly. Many free online PDF tools store your uploads on their servers indefinitely, which is a real privacy risk when your PDF contains an Aadhaar scan, a PAN card, a bank statement, or a medical report. RepetiGo is built differently.
When you remove pages from a PDF online with RepetiGo, your file is processed through a secure, temporary pipeline and then permanently deleted within 60 minutes. There is no sign-up, so we never link the file to your name or email. Nothing is kept, sold, or shared.
🔒 Your PDF is encrypted in transit, processed in an ephemeral workspace, and cryptographically deleted within 60 minutes. No account. No tracking. Read more on our security page.

H2: Remove Pages from a PDF on Any Device
Because RepetiGo runs in your browser, you can remove pages from a PDF on a laptop, phone, or tablet - no app to download. Here's how it works on each platform.
H3: On Mac (Preview + RepetiGo)
On a Mac you can open the PDF in Preview, show thumbnails from the View menu, select a page, and press Delete. That works for quick edits. But when you need to remove pages from a PDF on Mac and keep the file private - or Preview refuses to save a locked PDF - open RepetiGo in Safari or Chrome, upload the file, delete the pages, and download. No Preview quirks, no saved copies left behind.
H3: On iPhone
To remove pages from a PDF on iPhone, open RepetiGo in Safari, upload the PDF from Files or iCloud Drive, tap the pages you want gone, and download the new file straight back to your phone. There's no app to install and nothing is saved to our servers.
H3: On Android
The steps to remove pages from a PDF on Android are the same: open RepetiGo in Chrome, upload your PDF, select and delete the unwanted pages, and download. It works on any Android phone or tablet, and your file auto-deletes within 60 minutes.
H3: On Windows - Without Adobe Acrobat
You do not need Adobe Acrobat to delete pages from a PDF on Windows. Acrobat's page-delete feature sits behind a paid Acrobat Pro subscription. With RepetiGo you can remove pages from a PDF without Acrobat completely free - just open the tool in Edge or Chrome, upload, delete, and download. That's the whole point of a free online tool.
✅ No Adobe Acrobat, no Foxit, no desktop install. Removing pages from a PDF should be free - and with RepetiGo it is.

H2: Removing Specific, Multiple, or Blank Pages
H3: Remove a Single Page
To remove one page from a PDF, upload the file, click the single thumbnail you want to delete, and hit Remove Pages. This is the fastest way to drop a cover sheet, a duplicate, or one bad scan.
H3: Remove Multiple or Specific Pages
You can remove multiple pages from a PDF in one go. Click every page you want to delete - they don't have to be next to each other - or select a continuous range. RepetiGo removes all of them at once and rebuilds the document. This is how you remove specific pages from a PDF without doing it one at a time.
H3: Remove Blank Pages from a PDF
Scanned documents often end up with blank pages between sheets. To remove blank pages from a PDF, upload the file, spot the empty thumbnails, select them, and remove. It's a quick way to clean up a scan before printing - and it saves paper at the print counter.

H2: Remove Pages from a PDF - Built for India
Indian documents are full of extra pages you don't need to print. An Aadhaar PDF from mAadhaar or DigiLocker may include an instructions page. A downloaded PAN card, an exam admit card from NTA (JEE, NEET, UGC), or a bank statement often carries a cover sheet or terms page. Printing all of it wastes money at the cyber cafe.
With RepetiGo you can remove pages from a PDF online free in India before you print - keep only the page that matters, and hand over a clean file at the shop. Because nothing is stored and no sign-up is needed, your Aadhaar or PAN scan never sits on a stranger's server.
⚠️ Under India's DPDP Act 2023, personal documents like Aadhaar and PAN deserve careful handling. Removing extra pages before printing - and using a tool that auto-deletes your file - keeps your data in your control.

H2: For Print Shops and Cyber Cafes
If you run a print shop, cyber cafe, or CSC centre, you deal with this every day: a customer sends a 10-page PDF but only needs page 3 printed. Trimming pages by hand for every job eats time. RepetiGo's PrintPilot automates the whole counter workflow - customers upload by QR code, the AI cleans and prepares the document, and files auto-delete after printing.
The free tools like remove pages from a PDF introduce your customers to RepetiGo. PrintPilot is the paid engine that runs your shop: secure QR upload, AI page cleanup, print queue, UPI payments, and privacy built in.
➜ See How PrintPilot Runs Your Print Shop
Automate uploads, page cleanup, and secure printing.

H2: Why Use RepetiGo to Remove PDF Pages?
• Free forever - no paywall to delete pages from a PDF, unlike Adobe Acrobat Pro.
• No sign-up - no email, no account, no tracking to remove pages from a PDF online.
• Private by design - files auto-delete within 60 minutes; nothing is stored.
• Any device - remove pages from a PDF on Mac, iPhone, Android, or Windows in the browser.
• Quality preserved - remaining pages keep their original resolution and layout.
• Made for India - handles Aadhaar, PAN, and exam PDFs with privacy in mind.

H2: Frequently Asked Questions
H3: How do I remove pages from a PDF for free?
Upload your PDF to RepetiGo's remove pages from PDF tool, click the page thumbnails you want to delete, press Remove Pages, and download the new file. It's completely free, needs no sign-up, and works in any browser. Your original file is automatically deleted within 60 minutes, so nothing is stored.
H3: How do I remove pages from a PDF without Adobe Acrobat?
You don't need Adobe Acrobat. Acrobat's delete-page feature requires a paid Acrobat Pro subscription. RepetiGo lets you remove pages from a PDF without Acrobat for free - open the tool in any browser, upload your file, select the pages to delete, and download. No install and no licence required.
H3: Is it safe to remove pages from a PDF online?
With RepetiGo, yes. Your file is uploaded over an encrypted connection, processed in a temporary workspace, and permanently deleted within 60 minutes. There is no sign-up, so your PDF is never linked to your name or email. This matters most for sensitive files like Aadhaar, PAN, or bank statements.
H3: Can I remove multiple pages from a PDF at once?
Yes. Click every page you want to delete - they can be scattered across the document or a continuous range - and RepetiGo removes them all in a single step, then rebuilds a clean PDF. You can remove specific pages from a PDF without deleting them one by one.
H3: How do I remove pages from a PDF on Mac or iPhone?
On Mac you can use Preview's thumbnail view and press Delete, or use RepetiGo in Safari to keep the file private. On iPhone, open RepetiGo in Safari, upload the PDF from Files, tap the pages to remove, and download. Both work without installing any app.
H3: How do I remove blank pages from a PDF?
Upload the PDF to RepetiGo, look at the page thumbnails, select the empty ones, and click Remove Pages. Scanned documents often include blank pages between sheets - removing them cleans up the file and saves paper when you print.
H3: Will removing pages reduce the quality of my PDF?
No. Removing pages only deletes the pages you select. The remaining pages keep their original resolution, text, and formatting. The file usually gets smaller because it now contains fewer pages, but nothing about the kept pages changes.
H3: Do you keep a copy of my PDF after I remove pages?
No. RepetiGo processes your file through an ephemeral pipeline and deletes it automatically within 60 minutes. We don't require an account, we don't store your document, and we never sell or share your data.

H2: Remove Pages from Your PDF Now
Delete the pages you don't need and download a clean PDF in under a minute - free, private, and on any device.
➜ Remove Pages from PDF - Start Free
No sign-up · Auto-deletes in 60 minutes · Works on any device`;

const faqStart = content.indexOf("H2: Frequently Asked Questions");
const faqEnd = content.indexOf("H2: Remove Pages from Your PDF Now", faqStart);
const faqSchemaQuestions = Array.from(
  content.slice(faqStart, faqEnd).matchAll(/H3: ([^\n]+)\n([\s\S]*?)(?=\nH3: |\nH2:|$)/g),
).map((match) => [match[1], match[2].trim()] as const);

export default function RemovePagesPage() {
  return (
    <RemovePagesClient
      uploadTitle="Remove Pages from PDF - Free, Online, No Sign-Up"
      uploadDescription="Need to delete a page from a PDF? Upload your file, pick the pages you don't want, and download a clean new PDF."
      uploadHeadingLevel="h1"
    >
      <JsonLd />
      <article className="tool-seo-content" id="remove-pages-guide">
        <StructuredSeoCopy content={content} skipFirstH1 />
      </article>
    </RemovePagesClient>
  );
}

function StructuredSeoCopy({ content, skipFirstH1 = false }: { content: string; skipFirstH1?: boolean }) {
  const blocks = content
    .replace(/(^|\n)(H[123]: [^\n]+)\n/g, "$1\n$2\n\n")
    .split(/\n{2,}/)
    .map((block) => block.trim())
    .filter(Boolean);

  let firstH1Skipped = false;
  return (
    <>
      {blocks.map((block, index) => {
        const lines = block.split("\n").map((line) => line.trim()).filter(Boolean);
        const first = lines[0] || "";

        if (first.startsWith("H1: ")) {
          if (skipFirstH1 && !firstH1Skipped) {
            firstH1Skipped = true;
            return lines.length > 1 ? (
              <div className="tool-seo-copy-paragraph tool-seo-hero" key={index}>
                {renderLines(lines.slice(1), `${index}`)}
              </div>
            ) : null;
          }
          return <h1 key={index}>{first.slice(4)}</h1>;
        }
        if (first.startsWith("H2: ")) return <h2 key={index}>{first.slice(4)}</h2>;
        if (first.startsWith("H3: ")) return <h3 key={index}>{first.slice(4)}</h3>;

        return (
          <div className="tool-seo-copy-paragraph" key={index}>
            {renderLines(lines, `${index}`)}
          </div>
        );
      })}
    </>
  );
}

function renderLines(lines: string[], keyPrefix: string): ReactNode[] {
  const output: ReactNode[] = [];
  let index = 0;
  while (index < lines.length) {
    const line = lines[index];

    if (line.startsWith("✓")) {
      output.push(
        <div className="tool-seo-badges" key={`${keyPrefix}-badges-${index}`}>
          {line.split(/\s{2,}/).map((item) => <span key={item}>{item}</span>)}
        </div>,
      );
      index += 1;
      continue;
    }

    if (isCallout(line)) {
      output.push(<aside className="tool-seo-callout" key={`${keyPrefix}-callout-${index}`}><p>{renderInline(line)}</p></aside>);
      index += 1;
      continue;
    }

    if (/^\d+\.\s/.test(line)) {
      const items: string[] = [];
      while (index < lines.length && /^\d+\.\s/.test(lines[index])) {
        items.push(lines[index].replace(/^\d+\.\s*/, ""));
        index += 1;
      }
      output.push(<ol className="tool-seo-list" key={`${keyPrefix}-steps-${index}`}>{items.map((item) => <li key={item}>{renderInline(item)}</li>)}</ol>);
      continue;
    }

    if (line.startsWith("•")) {
      const items: string[] = [];
      while (index < lines.length && lines[index].startsWith("•")) {
        items.push(lines[index].replace(/^•\s*/, ""));
        index += 1;
      }
      output.push(<ul className="tool-seo-list" key={`${keyPrefix}-list-${index}`}>{items.map((item) => <li key={item}>{renderInline(item)}</li>)}</ul>);
      continue;
    }

    if (line.startsWith("➜")) {
      const label = line.replace(/^➜\s*/, "");
      const helper = lines[index + 1] && !isSpecialLine(lines[index + 1]) ? lines[index + 1] : "";
      output.push(
        <div className="tool-seo-cta-stack" key={`${keyPrefix}-cta-${index}`}>
          <a className="tool-seo-inline-cta" href={label.startsWith("See How PrintPilot") ? "/print-automation" : "/pdf-tools/remove-pages"}>{label} <span>→</span></a>
          {helper ? <p>{helper}</p> : null}
        </div>,
      );
      index += helper ? 2 : 1;
      continue;
    }

    output.push(<p key={`${keyPrefix}-paragraph-${index}`}>{renderInline(line)}</p>);
    index += 1;
  }
  return output;
}

function isSpecialLine(line: string) {
  return line.startsWith("H") || line.startsWith("✓") || line.startsWith("➜") || line.startsWith("•") || isCallout(line) || /^\d+\.\s/.test(line);
}

function isCallout(line: string) {
  return ["📊", "💡", "🔒", "✅", "⚠"].some((symbol) => line.startsWith(symbol));
}

function renderInline(text: string): ReactNode[] {
  const patterns = /(security page|PrintPilot|RepetiGo's Remove Pages tool|remove pages from PDF tool|Remove Pages tool)/g;
  return text.split(patterns).map((part, index) => {
    if (part === "security page") return <a href="/privacy-policy" key={`${part}-${index}`}>security page</a>;
    if (part === "PrintPilot") return <a href="/print-automation" key={`${part}-${index}`}>PrintPilot</a>;
    if (part === "RepetiGo's Remove Pages tool" || part === "remove pages from PDF tool" || part === "Remove Pages tool") {
      return <a href="/pdf-tools/remove-pages" key={`${part}-${index}`}>{part}</a>;
    }
    return part;
  });
}

function JsonLd() {
  const schemas = [
    {
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      name: "RepetiGo Remove Pages from PDF",
      applicationCategory: "UtilitiesApplication",
      operatingSystem: "Web",
      offers: { "@type": "Offer", price: "0", priceCurrency: "INR" },
      description: "Free online tool to delete single, multiple, specific, or blank pages from a PDF.",
      url: pageUrl,
    },
    {
      "@context": "https://schema.org",
      "@type": "HowTo",
      name: "How to Remove Pages from a PDF",
      step: [
        { "@type": "HowToStep", name: "Upload", text: "Upload your PDF file." },
        { "@type": "HowToStep", name: "Select", text: "Select the pages to delete." },
        { "@type": "HowToStep", name: "Remove", text: "Click Remove Pages." },
        { "@type": "HowToStep", name: "Download", text: "Download your new PDF." },
      ],
    },
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      mainEntity: faqSchemaQuestions.map(([question, answer]) => ({
        "@type": "Question",
        name: question,
        acceptedAnswer: { "@type": "Answer", text: answer },
      })),
    },
    {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      itemListElement: [
        { "@type": "ListItem", position: 1, name: "Home", item: "https://repetigo.com/" },
        { "@type": "ListItem", position: 2, name: "PDF Tools", item: "https://repetigo.com/pdf-tools" },
        { "@type": "ListItem", position: 3, name: "Remove Pages", item: pageUrl },
      ],
    },
  ];

  return (
    <>
      {schemas.map((schema) => (
        <script key={schema["@type"]} type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} />
      ))}
    </>
  );
}
