import type { Metadata } from "next";
import ConvertToJpgClient from "./ConvertToJpgClient";

const pageUrl = "https://repetigo.com/image-tools/convert-to-jpg";

export const metadata: Metadata = {
  title: "Convert to JPG Online Free - PNG, WebP, GIF, HEIC & More | RepetiGo",
  description:
    "Convert to JPG free online from PNG, WebP, GIF, BMP, SVG, or HEIC - one tool, every format. Batch convert multiple files. No sign-up, no watermark, 100% browser-based - nothing is ever uploaded.",
  alternates: { canonical: pageUrl },
  openGraph: {
    title: "Convert to JPG Online Free - PNG, WebP, GIF, HEIC & More | RepetiGo",
    description: "Convert PNG, WebP, GIF, BMP, SVG, or HEIC to JPG free - one tool, every format. Batch convert. No sign-up, 100% browser-based.",
    type: "website",
    url: pageUrl,
  },
  twitter: {
    card: "summary_large_image",
    title: "Convert to JPG Free - RepetiGo",
    description: "Convert any image format to JPG free, one file or a batch. No sign-up, 100% browser-based.",
  },
  robots: { index: true, follow: true },
};

const content = String.raw`H1: Convert to JPG Online Free. PNG, WebP, GIF, BMP, SVG, or HEIC - Every Format, One Tool.
RepetiGo's free Convert to JPG tool turns almost any image format into JPG from a single upload box - PNG, WebP, GIF, BMP, SVG, or HEIC (iPhone photos) all work without picking a separate converter first. Upload one file or an entire batch, and every file converts to JPG automatically. No account, no watermark, and your files are never uploaded anywhere - conversion runs entirely inside your browser.
JPG remains the format that Indian government portals, banking applications, scholarship forms, job portals, and print shops expect by default. Whatever format your image currently is, this tool gets it to JPG in one step.

✓ Accepts PNG, WebP, GIF, BMP, SVG & HEIC   ✓ Batch Convert Multiple Files   ✓ Adjustable Quality   ✓ No Sign-Up   ✓ 100% Browser-Based - Nothing Uploaded

[ Convert to JPG Free - No Sign-Up → repetigo.com/image-tools/convert-to-jpg ]

H2: Which Formats Can You Convert to JPG?
This single upload box accepts every common image format and converts each one to JPG the same way - fill any transparent areas with white, then re-encode as JPG at your chosen quality. Here's what to expect from each source format:

H3: Convert to JPG - Format Support Table
Source Format
Common Extension
Transparency in Source
What Happens When Converted to JPG
PNG
.png
Often transparent (logos, screenshots, graphics)
Transparent areas fill white - correct, expected behaviour for JPG
WebP
.webp
Sometimes transparent (modern web images, downloaded icons)
Transparent areas fill white; photos convert with no visible change
GIF
.gif
1-bit transparency only (fully on or off)
Transparent areas fill white; only the first frame is used if animated
BMP
.bmp
Rare - usually opaque
Converts directly - BMP is uncompressed, so JPG is almost always much smaller
SVG
.svg
Often transparent (icons, logos, line art)
Rendered to a raster image first, then transparent areas fill white
HEIC / HEIF
.heic / .heif
Not typically used - iPhone photos are usually opaque
Decoded and converted directly - this is the standard iPhone-to-JPG workflow

💡 If your source image has a transparent background you need to keep, don't convert it to JPG - JPG cannot store transparency at all. Use RepetiGo's WebP to PNG or PNG Converter tools instead, which preserve transparency in the output.

H2: How to Convert to JPG Online Free in 3 Steps.
H3: Step 1 - Upload Your Image
Click Select Images or drag and drop your file - PNG, WebP, GIF, BMP, SVG, or HEIC/HEIF all work in the same upload box, and you can mix formats in a single batch. There's no fixed file size cap - because everything runs on your own device, very large files just take a little longer.
H3: Step 2 - Convert to JPG
RepetiGo converts automatically at a 90% starting quality as soon as you upload. Drag the quality slider between 40% and 100% - higher keeps more detail, lower makes a smaller file. For a batch, the same quality setting applies to every file, whatever format each one started as.
H3: Step 3 - Download Your JPG
Click Download for a single file, or Download ZIP for a batch to get every converted JPG in one archive. File names are preserved with a new extension (photo.png → photo.jpg, image.heic → image.jpg). Because nothing was ever uploaded, there's nothing left on any server once you're done.

[ Convert to JPG Free Now → repetigo.com/image-tools/convert-to-jpg ]

H2: ★ Batch Convert to JPG.
Most real conversion jobs aren't a single file - they're a folder of screenshots, a set of downloaded icons, or a mixed batch of images collected from different places in different formats. RepetiGo's batch converter handles all of it in one session:
Upload PNG, WebP, GIF, BMP, SVG, and HEIC files together in the same batch - the tool detects each format automatically.
Every file converts to JPG at the same quality setting, one operation regardless of file count.
Download ZIP to get one archive containing every converted JPG.
Extract the zip - all JPGs named identically to the originals, just with a .jpg extension.

💡 You don't need to sort files by format before uploading. Drop a mixed folder of PNGs, WebPs, and HEIC photos into the upload box together - RepetiGo converts each one correctly based on what it actually is, not what folder it came from.

H2: ★ Indian Use Cases - Who Needs to Convert to JPG in India?
JPG is the one format nearly every Indian portal, bank, and print workflow can rely on - which means a constant stream of PNG, WebP, GIF, and HEIC files need converting before they're usable:

Who Uses It
Common Situation
Why JPG
Students and job applicants
A scanned certificate or ID photo saved as PNG or downloaded as WebP
Government portals (NTA, SSC, IBPS, scholarship sites) require JPG and often reject other formats outright
iPhone users
Camera roll photos saved as HEIC by default
Windows PCs, Android phones, and virtually all Indian portals can't open HEIC directly
Print shops and cyber cafes
Customer logos or graphics arriving as PNG, WebP, or GIF via WhatsApp
Older printing and layout software handles JPG more reliably than newer formats
Web developers
WebP images downloaded from a site that need a universal format for reuse
JPG opens in any image viewer, editor, or legacy tool without compatibility questions
Office and admin staff
Old graphics or clip art still saved as GIF or BMP
JPG is smaller than BMP and more universally supported than GIF for photos

H2: Why Use RepetiGo's Convert to JPG Tool?
Feature
RepetiGo
CloudConvert
ILoveIMG
Windows Photos
Free to use
✓ Always free
✓ Free (25 conversions/day)
✓ Limited free
✓ Free (built-in)
Accepts multiple source formats in one tool
✓ PNG, WebP, GIF, BMP, SVG, HEIC
✓ Many formats
✓ Several formats
~ Format support varies
Sign-up required
✓ Never
✗ Account for higher limits
~ Optional
✓ No account
Batch conversion
✓ Yes - mixed formats in one batch
✓ Yes (limit applies)
✓ Yes
✗ One at a time (export)
Files ever leave your device?
✗ Never - 100% browser-based
✓ Yes - uploaded to their servers
✓ Yes - uploaded to their servers
✓ Never - local
No watermark on output
✓ Always
✓ Yes
✓ Yes
✓ Yes

H2: Your Files Are Safe. Always.
Protection Layer
What It Means in Practice
🖥️ 100% Browser-Based Conversion
Your image is converted using your own device's processing power via the browser. It is never uploaded to any server.
🚫 Nothing Ever Leaves Your Device
Because conversion happens locally, there is no upload, no transfer, and nothing on any server for us to store or delete.
👁️ No Image Content Is Read
RepetiGo's code cannot see, analyse, or extract what's in your image - it only re-encodes pixel data on your own device.
🚫 No Account = No Data Profile
No sign-up means no personal data, no file history, and no usage profile is ever created.
🔒 Well Suited to ID Photos and Personal Documents
Because nothing is transmitted anywhere, this is one of the safer ways to convert a sensitive scan or ID photo online.

[ Read Our Privacy Policy → /privacy-policy ]

H2: Common Questions About Converting to JPG.
H3: Q1: Can I Upload Different File Formats in the Same Batch?
Yes. You can select or drop a mix of PNG, WebP, GIF, BMP, SVG, and HEIC files together in a single upload, and RepetiGo converts each one to JPG correctly based on its actual format - you don't need to separate them first or run multiple batches for different formats.
H3: Q2: What Happens to Transparency When I Convert to JPG?
JPG cannot store transparency at all. Any transparent or semi-transparent area in your source PNG, WebP, GIF, or SVG file is filled with solid white in the JPG output - this is correct, expected behaviour, not an error. If you need to keep transparency, don't convert to JPG - use a PNG or WebP output tool instead, such as RepetiGo's WebP to PNG converter.
H3: Q3: Does Converting to JPG Reduce Image Quality?
It depends on the setting and the source format. At quality 90-100, the visual difference from the source is generally not noticeable. Converting from a lossless source (PNG, BMP) to JPG always involves some quality trade-off because JPG is a lossy format - that's the trade for a smaller file. Converting from GIF doesn't lose additional quality in a meaningful way, since GIF's 256-colour palette already limits detail before conversion even starts.
H3: Q4: Does This Tool Convert Animated GIFs?
This tool produces a single static JPG. If you upload an animated GIF, only its first frame is used for the JPG output - none of the animation carries over, because JPG can't store multiple frames. If you need to keep the animation, don't convert it to JPG; keep the file as a GIF instead.
H3: Q5: Can I Convert a HEIC Photo From My iPhone to JPG on This Page?
Yes. Upload your .heic or .heif file the same way as any other format - RepetiGo decodes it in your browser and converts it to JPG, which is the standard workflow for getting an iPhone photo into a format that Windows, Android, and Indian government portals can all open. For a dedicated HEIC-focused walkthrough with Windows, Mac, and Android instructions, see RepetiGo's HEIC to JPG converter.

H2: More Free Image Tools from RepetiGo.
Tool
What It Does
Link
HEIC to JPG
Dedicated iPhone photo converter with Windows, Mac, and Android guidance
→ /image-tools/heic-to-jpg
PNG to JPG
Dedicated PNG to JPG converter with quality control
→ /image-tools/png-to-jpg
WebP to JPG
Dedicated WebP to JPG converter for web images and downloads
→ /image-tools/webp-to-jpg
Convert from JPG
Reverse direction - convert JPG into PNG or WebP
→ /image-tools/convert-from-jpg
Compress Image
Reduce JPG file size further after conversion
→ /image-tools/compress-image
All Image Tools
Complete free image tools suite
→ /image-tools

[ Convert to JPG Free - No Sign-Up → repetigo.com/image-tools/convert-to-jpg ]
[ Explore All Image Tools → repetigo.com/image-tools ]`;

const faqSchemaQuestions = Array.from(content.matchAll(/H3: (Q\d+: [^\n]+)\n([\s\S]*?)(?=\nH3: Q\d+:|\nH2:|$)/g)).map((match) => [match[1], match[2].trim()] as const);

export default function ConvertToJpgPage() {
  return (
    <ConvertToJpgClient>
      <JsonLd />
      <article className="tool-seo-content compress-pdf-seo" id="convert-to-jpg-guide">
        <StructuredSeoCopy content={content} />
      </article>
    </ConvertToJpgClient>
  );
}

type SeoTableData = { headers: string[]; rows: string[][] };
const CALLOUT_EMOJI = ["💡", "🇮🇳", "🔒", "🖨️", "📱", "✅", "⚠️", "🖥️", "🔁", "🔄", "★"];

function StructuredSeoCopy({ content: source }: { content: string }) {
  const blocks = source.replace(/(^|\n)(H[123]: [^\n]+)\n/g, "$1\n$2\n\n").split(/\n{2,}/).map((block) => block.trim()).filter(Boolean);
  return (
    <>
      {blocks.map((block, index) => {
        const lines = block.split("\n").map((line) => line.trim()).filter(Boolean);
        const first = lines[0];
        if (first.startsWith("H1: ")) return <h1 key={index}>{first.slice(4)}</h1>;
        if (first.startsWith("H2: ")) return <h2 key={index}>{first.slice(4)}</h2>;
        if (first.startsWith("H3: ")) {
          const heading = first.slice(4);
          const body = lines.slice(1);
          return (
            <section className="tool-seo-copy-block" key={index}>
              <h3>{heading}</h3>
              {body.map((line) => <p key={line}>{renderInlineMappedLinks(line)}</p>)}
            </section>
          );
        }
        const table = getKnownTable(lines);
        if (table) return <SeoTable key={index} {...table} />;
        if (first.startsWith("✓ ")) {
          return <div className="tool-seo-badges" key={index}>{first.split(/\s{2,}/).map((item) => <span key={item}>{item}</span>)}</div>;
        }
        if (lines.length && lines.every((line) => line.startsWith("[ ") && line.endsWith(" ]"))) {
          return <div className="tool-seo-cta-stack" key={index}>{lines.map((line) => <CtaLine key={line} text={line} />)}</div>;
        }
        if (CALLOUT_EMOJI.some((emoji) => first.startsWith(emoji))) {
          return <aside className="tool-seo-callout" key={index}>{lines.map((line) => <p key={line}>{renderInlineMappedLinks(line)}</p>)}</aside>;
        }
        return <div className="tool-seo-copy-paragraph" key={index}>{lines.map((line) => <p key={line}>{renderInlineMappedLinks(line)}</p>)}</div>;
      })}
    </>
  );
}

function getKnownTable(lines: string[]): SeoTableData | null {
  if (lines[0] === "Source Format" && lines[1] === "Common Extension") return { headers: ["Source Format", "Common Extension", "Transparency in Source", "What Happens When Converted to JPG"], rows: chunkRows(lines.slice(4), 4) };
  if (lines[0] === "Who Uses It" && lines[1] === "Common Situation") return { headers: ["Who Uses It", "Common Situation", "Why JPG"], rows: chunkRows(lines.slice(3), 3) };
  if (lines[0] === "Feature" && lines[1] === "RepetiGo") return { headers: ["Feature", "RepetiGo", "CloudConvert", "ILoveIMG", "Windows Photos"], rows: chunkRows(lines.slice(5), 5) };
  if (lines[0] === "Protection Layer" && lines[1] === "What It Means in Practice") return { headers: ["Protection Layer", "What It Means in Practice"], rows: chunkRows(lines.slice(2), 2) };
  if (lines[0] === "Tool" && lines[1] === "What It Does" && lines[2] === "Link") return { headers: ["Tool", "What It Does", "Link"], rows: chunkRows(lines.slice(3), 3) };
  return null;
}

function chunkRows(values: string[], size: number) {
  const rows: string[][] = [];
  for (let index = 0; index < values.length; index += size) rows.push(values.slice(index, index + size));
  return rows;
}

function SeoTable({ headers, rows }: SeoTableData) {
  return (
    <div className="tool-seo-table-wrap">
      <table>
        <thead><tr>{headers.map((header) => <th key={header}>{header}</th>)}</tr></thead>
        <tbody>
          {rows.map((row) => <tr key={row.join("|")}>{row.map((cell, index) => <td key={cell + "-" + index}>{renderTableCell(cell)}</td>)}</tr>)}
        </tbody>
      </table>
    </div>
  );
}

function CtaLine({ text }: { text: string }) {
  const inner = text.slice(2, -2);
  const [, label = inner, href = ""] = inner.match(/^(.*?)\s*(?:→)\s*(.+)$/) || [];
  const mappedHref = mapSeoRoute(href || "");
  return <a className="tool-seo-inline-cta" href={mappedHref || "#convert-to-jpg-guide"}>{label}{mappedHref ? <span>{"→"}</span> : null}</a>;
}

function renderTableCell(cell: string) {
  const cleaned = cell.replace(/^→\s*/, "").trim();
  const href = mapSeoRoute(cleaned);
  if (!href) return renderInlineMappedLinks(cell);
  return <a className="tool-seo-table-link" href={href}>{getRouteLabel(href)}</a>;
}

function renderInlineMappedLinks(text: string) {
  const parts = text.split(/(repetigo\.com\/(?:image-tools\/[a-z-]*|pricing)\/?|\/image-tools\/[a-z-]*\/?|\/image-tools\/?|\/products\/printpilot\/?|\/privacy-policy\/?|\/pricing\/?)/g);
  return parts.map((part, index) => {
    const href = mapSeoRoute(part.startsWith("repetigo.com") ? "https://" + part : part);
    if (!href) return part;
    return <a href={href} key={part + "-" + index}>{getRouteLabel(href)}</a>;
  });
}

function mapSeoRoute(route: string) {
  const cleanRoute = route.trim().replace(/^(https?:\/\/)?(www\.)?repetigo\.com/i, "").replace(/\/$/, "");
  const routeMap: Record<string, string> = {
    "/image-tools": "/image-tools",
    "/image-tools/convert-to-jpg": "/image-tools/convert-to-jpg",
    "/image-tools/heic-to-jpg": "/image-tools/heic-to-jpg",
    "/image-tools/png-to-jpg": "/image-tools/png-to-jpg",
    "/image-tools/webp-to-jpg": "/image-tools/webp-to-jpg",
    "/image-tools/convert-from-jpg": "/image-tools/convert-from-jpg",
    "/image-tools/compress-image": "/image-tools/compress-image",
    "/products/printpilot": "/print-automation",
    "/privacy-policy": "/privacy-policy",
    "/pricing": "/pricing",
  };
  return routeMap[cleanRoute] || (cleanRoute.startsWith("/image-tools") ? cleanRoute : "");
}

function getRouteLabel(href: string) {
  const labels: Record<string, string> = {
    "/image-tools": "Explore All Image Tools",
    "/image-tools/convert-to-jpg": "Open Convert to JPG",
    "/image-tools/heic-to-jpg": "Open HEIC to JPG",
    "/image-tools/png-to-jpg": "Open PNG to JPG",
    "/image-tools/webp-to-jpg": "Open WebP to JPG",
    "/image-tools/convert-from-jpg": "Open Image Format Converter",
    "/image-tools/compress-image": "Open Compress Image",
    "/print-automation": "Learn About PrintPilot",
    "/privacy-policy": "Read Privacy Policy",
    "/pricing": "Start Free Trial",
  };
  return labels[href] || "Open Tool";
}

function JsonLd() {
  const softwareApplication = { "@context": "https://schema.org", "@type": "SoftwareApplication", name: "RepetiGo Convert to JPG", applicationCategory: "UtilitiesApplication", operatingSystem: "Web", offers: { "@type": "Offer", price: "0", priceCurrency: "INR" }, description: "Free online converter that turns PNG, WebP, GIF, BMP, SVG, or HEIC images into JPG, with batch conversion. Runs entirely in the browser - no file is ever uploaded to a server.", url: pageUrl };
  const howTo = { "@context": "https://schema.org", "@type": "HowTo", name: "How to Convert to JPG Online Free", step: [{ "@type": "HowToStep", name: "Upload", text: "Upload Your Image" }, { "@type": "HowToStep", name: "Convert", text: "Convert to JPG" }, { "@type": "HowToStep", name: "Download", text: "Download Your JPG" }] };
  const faqPage = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: faqSchemaQuestions.map(([question, answer]) => ({ "@type": "Question", name: question, acceptedAnswer: { "@type": "Answer", text: answer } })) };
  const breadcrumb = { "@context": "https://schema.org", "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Home", item: "https://repetigo.com/" }, { "@type": "ListItem", position: 2, name: "Image Tools", item: "https://repetigo.com/image-tools" }, { "@type": "ListItem", position: 3, name: "Convert to JPG", item: pageUrl }] };

  return <>{[softwareApplication, howTo, faqPage, breadcrumb].map((schema) => <script key={schema["@type"]} type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} />)}</>;
}
